import {Api, ApiListResponse} from '../api';
import {IEvents} from '../events';



export class ProductService<T>{

    constructor(private api: Api, private event:IEvents){
    }

    async getAll(){
        const res = await this.api.get('api/weblarek/products') as ApiListResponse<ApiProduct> 
    }


}